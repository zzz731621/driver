1.  什麼是unlocked_ioctl接口
    實際上就是ioctl接口，功能和對應的system call都一樣。

2.  unlocked_ioctl和read/write函數有什麼相同點和不同點?
    相同:
        都能夠從kernel中讀寫數據
    不同:
        read函數只能完成讀的功能，write只能完成寫的功能，在讀取大量數據時效率較高。
        ioctl既能讀又能寫，在讀取大量數據時效率較低。

3.  unlocked_ioctl接口命令規則。
    是一個unsigned int分成4區
    0~7位:  命令的編號，範圍是0-255
    8~15位: 命令的magic number
    第一個和第二個分區主要是用來區分命令的。

    16~29位:表示傳遞的數據大小。
    30~31位:代表讀寫的方向，因為ioctl既能讀也能寫。
    00:表示應用程式和驅動程式沒有數據傳遞。
    10:表示應用程式從驅動程式讀數據。
    01:表示應用程式向驅動程式寫數據。
    00:表示應用程式先向驅動程式寫數據，再從驅動把數據讀出來，實際上用的不多。

4.  ioctl命令的macro
    命令合成:
    _IO(type,nr):       用來定義沒有數據傳遞的命令
    _IOR(type,nr,size): 用來定義從驅動中讀取數據的命令
    _IOW(type,nr,size): 用來定義向驅動中寫入數據的命令
    _IOWR(type,nr,size): 用來定義數據交換類型的命令，先向驅動寫入數據，在讀取數據
    參數:
    type:   表示命令組成的magic number，8~15位
    nr:     表示命令組成的編號，0~7位
    size:   表示命令組成的參數傳遞大小，要寫的是數據類型而不是數字，例如要傳入4bytes就填入int
    這四個macro本身已經有讀寫方向了，所以不用填參數30~31位，例如_IOR就是10

    命令分解:
    _IOC_DIR(nr):       分解命令的方向，也就是30~31位
    _IOC_TYPE(nr):      分解命令的magic number，也就是8~15位
    _IOC_NR(nr):        分解命令的編號，也就是0~7位
    _IOC_SIZE(nr):      分解命令的數據大小，也就是16~29位
    參數:
    nr:     要分解的命令
